---
import { getCollection } from 'astro:content';
const posts = await getCollection('blog')
---

<style>
    @import url('https://fonts.googleapis.com/css2?family=Rouge+Script&display=swap');
</style>

<blog-home>
    <h1 class="font-[Rouge_Script] text-6xl xl:text-8xl bg-linear-to-br from-accent to-primary bg-clip-text text-transparent text-center pt-8 pb-7">
    a Taste of Chikun
  </h1>
  <div class="prose">
    <p>
      Hahaha, wtf no.  I'll come up with another name later.
    </p>
    <div class="divider divider-primary">
      <span class="text-accent font-bold">DEV POSTS</span>
    </div>
    <ul>
      { posts.map(post => (
        <li><a href={`/blog/posts/${post.id}`}>{post.data.title}</a> - {post.id}</li>
      ))}
    </ul>
  </div>
</blog-home>